---
---
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <title>🔒 ADMIN LOGIN</title>
    <style>
      body { background: #0c0c0c; color: #00ff41; font-family: 'Courier New', monospace; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; }
      .login-box { border: 2px solid #00ff41; padding: 40px; text-align: center; box-shadow: 0 0 15px #00ff41; }
      input { background: #000; border: 1px solid #00ff41; color: #00ff41; padding: 10px; width: 200px; outline: none; text-align: center; font-size: 1.1rem; }
      button { background: #00ff41; color: #000; border: none; padding: 10px 20px; margin-top: 20px; cursor: pointer; font-weight: bold; }
      button:hover { background: #fff; }
      #admin-content { display: none; } /* 처음엔 숨김 */
      .hidden { display: none !important; }
      code { color: #ffff00; }
    </style>
  </head>
  <body>

    <div id="login-form" class="login-box">
      <h2>[ ACCESS DENIED ]</h2>
      <p>관리자 인증 코드를 입력하세요.</p>
      <input type="password" id="password-input" placeholder="PASSCODE" onkeypress="if(event.keyCode==13) checkPassword()"/>
      <br />
      <button onclick="checkPassword()">ENTER</button>
    </div>

    <div id="admin-content" class="login-box" style="text-align: left; width: 600px;">
      <h1 style="color: #ff3e00;">🛠️ OPERATOR CONSOLE</h1>
      <p>> 관리자 권한이 승인되었습니다.</p>
      <hr style="border: 1px solid #333;" />
      
      <h3>1. 제보 내역 확인 (Google Sheets)</h3>
      <p>사용자들이 추천한 블로그 목록을 확인하세요.</p>
      <a href="네_구글_시트_주소" target="_blank" style="color: #fff; background: #444; padding: 8px 15px; text-decoration: none;">스프레드시트 열기</a>

      <h3>2. 데이터 업데이트 가이드</h3>
      <p>새 글을 등록하려면 <code>src/data.json</code> 파일에 형식을 맞춰 추가한 뒤 <b>GitHub Push</b> 하세요.</p>
      
      <button onclick="location.href='/otomadbloghub/'" style="background: #333; color: #888;">LOGOUT</button>
    </div>

    <script is:inline>
      // 💡 여기서 비밀번호를 설정해! (원하는 문자로 바꿔)
      const ADMIN_PASSCODE = "bozi4973"; 

      function checkPassword() {
        const input = document.getElementById('password-input').value;
        const loginForm = document.getElementById('login-form');
        const adminContent = document.getElementById('admin-content');

        if (input === ADMIN_PASSCODE) {
          loginForm.classList.add('hidden');
          adminContent.style.display = 'block';
          alert("인증 성공: 접속을 허가합니다.");
        } else {
          alert("경고: 잘못된 패스코드입니다. 침입 시도가 기록되었습니다.");
        }
      }
    </script>
  </body>
</html>